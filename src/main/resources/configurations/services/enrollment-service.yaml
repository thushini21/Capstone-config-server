server:
  port: 8002

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/eca?createDatabaseIfNotExist=true
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
